export type Colors = {
  primary: string;
  background: string;
  card: string;
  text: string;
  border: string;
  notification: string;
  error: string;
  textPrimary: string;
  textSecondary: string;
};

export type Theme = {
  dark: boolean;
  colors: Colors;
};
